@model IEnumerable<Instituciones.Models.AntecedenteLaboral>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idAntLaboral }) |
            @Html.ActionLink("Details", "Details", new { id=item.idAntLaboral }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idAntLaboral })
        </td>
    </tr>
}

</table>
